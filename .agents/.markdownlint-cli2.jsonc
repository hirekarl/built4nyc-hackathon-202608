{
  // Skill instruction files start with YAML frontmatter followed by prose, not an H1 —
  // MD041 doesn't apply here. Mirrors .claude/.markdownlint-cli2.jsonc for the Codex-facing
  // skill twins under .agents/skills/.
  "config": {
    "MD041": false
  }
}
